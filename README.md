a simple app that:
- lists names froma a variable
- created a "ListItem" composable function to list each name and add card and text
- uses "Card" to create a card for each name
- uses "modifier" function to modify the size and padding of objects
- uses vector asset baseline_person_24 in place of a person's image
- used "Row" to place items in a row
